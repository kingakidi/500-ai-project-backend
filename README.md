## Create Super Admin

```bash
python manage.py create_superadmin
```

Requires environment variables:

- `SUPERADMIN_EMAIL`
- `SUPERADMIN_PASSWORD`
- `SUPERADMIN_FIRST_NAME` (optional)
- `SUPERADMIN_LAST_NAME` (optional)

# Finger Print AI Attendance Verification System (Backend)
